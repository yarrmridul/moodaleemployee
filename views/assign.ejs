<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Assign New Task</title>
  <link rel="stylesheet" href="/assign.css">
  <link
    href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700;900&family=Noto+Sans:wght@400;500;700;900&family=Material+Icons"
    rel="stylesheet">
</head>

<body>

  <h2 class="head1">Assign New Task</h2>

  <form class="task-form">
    <div>
      <label for="task-name" class="form-label">Task Name</label>
      <input id="task-name" type="text" class="form-input" placeholder="e.g., Design homepage mockup">
    </div>
    <div class="form-row">
      <div>
        <label for="priority" class="form-label">Priority</label>
        <select id="priority" class="form-input">
          <option value="">Select priority</option>
          <option value="low">Low</option>
          <option value="medium">Medium</option>
          <option value="high">High</option>
        </select>
      </div>
      <div>
        <label for="deadline" class="form-label">Deadline</label>
        <input id="deadline" type="date" class="form-input">
      </div>
    </div>
    <div>
      <label for="client-name" class="form-label">Client Name</label>
      <input id="client-name" type="text" class="form-input" placeholder="Enter client name">
    </div>
    <div>
      <label for="task-details" class="form-label">Task Details</label>
      <textarea id="task-details" class="form-input" rows="4"
        placeholder="Provide a detailed description of the task..."></textarea>
    </div>
    <div>
      <label for="assignee" class="form-label">Assignee (Employee Name or ID)</label>
      <input id="assignee" type="text" class="form-input" placeholder="Search by name or enter ID">
    </div>
    <div>
      <label class="form-label">Required Documents</label>
      <div class="file-upload">
        <span class="material-icons upload-icon">cloud_upload</span>
        <p>
          <label for="file-upload" class="upload-label">
            <span>Upload a file</span>
            <input id="file-upload" type="file" multiple class="sr-only">
          </label>
          <span class="upload-or"> or drag and drop</span>
        </p>
        <p class="upload-info">PNG, JPG, PDF up to 10MB</p>
      </div>
    </div>
    <div class="form-actions">
      <a href="/admin/task" class="btn btn-secondary">Cancel</a>
      <button type="submit" class="btn btn-primary">
        <span class="material-icons">assignment_turned_in</span>
        Assign Task
      </button>
    </div>
  </form>
</body>

</html>